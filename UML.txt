UML Diagram for ZIPRIDE Dispatch System



+--------------------------------------------------+
|                     Graph                        |
+--------------------------------------------------+
| - graph : dict                                   |
+--------------------------------------------------+
| + add_node(node)                                 |
| + add_edge(u, v, weight)                         |
| + display()                                      |
| + bfs(start)                                     |
| + dfs_cycle()                                    |
| + _dfs(current, visited, parent)                 |
| + dijkstra(start, end)                           |
+--------------------------------------------------+


+--------------------------------------------------+
|                  HashEntry                       |
+--------------------------------------------------+
| - key : int                                      |
| - value : string                                 |
+--------------------------------------------------+
| + __init__(key, value)                           |
+--------------------------------------------------+


+--------------------------------------------------+
|                  HashTable                       |
+--------------------------------------------------+
| - size : int                                     |
| - table : list                                   |
| - count : int                                    |
+--------------------------------------------------+
| + hash_function(key)                             |
| + insert(key, value)                             |
| + search(key)                                    |
| + delete(key)                                    |
| + display()                                      |
+--------------------------------------------------+


+--------------------------------------------------+
|                    MaxHeap                       |
+--------------------------------------------------+
| - heap : list                                    |
+--------------------------------------------------+
| + insert(item)                                   |
| + heapify_up(index)                              |
| + extract_priority()                             |
| + heapify_down(index)                            |
| + peek()                                         |
+--------------------------------------------------+


+--------------------------------------------------+
|                  sorting.py                      |
+--------------------------------------------------+
| + merge_sort(arr)                                |
| + quick_sort(arr, low, high)                     |
| + partition(arr, low, high)                      |
+--------------------------------------------------+


+--------------------------------------------------+
|                    main.py                       |
+--------------------------------------------------+
| + main()                                         |
+--------------------------------------------------+
| Uses:                                            |
| -> Graph                                         |
| -> HashTable                                     |
| -> MaxHeap                                       |
| -> merge_sort()                                  |
| -> quick_sort()                                  |
+--------------------------------------------------+